# Copy to `.env` and edit. Never commit real credentials.

# IBKR / TWS / IB Gateway
IB_HOST=127.0.0.1
IB_PORT=7496
IB_CLIENT_ID=0

# Folio mobile server (HTTPS + PWA)
MOBILE_PORT=7500

# Optional: used only by some scripts under `projects/`
OPENAI_API_KEY=your_key_here

